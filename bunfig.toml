# Project overrides (~/.bunfig.toml shallow merge)
# Enable committed lockfile for reproducible installs; relax frozen lockfile for local dev.
[install]
saveTextLockfile = true
frozenLockfile = false
